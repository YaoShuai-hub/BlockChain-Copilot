# BlockChain-Copilot Config (TOML)

# Server port (if running# Network Configuration - NILE TESTNET)
port = 8080

# TronGrid / TronScan Base URLs - NILE TESTNET
tronscan_base = "https://nileapi.tronscan.org/api"
trongrid_base = "https://nile.trongrid.io"

# TronGrid / TronScan API Keys (Overrides .env if set here, but .env is preferred for secrets)
tronscan_api_key = "1722e8d8-b07a-44e7-8900-8e78db77d373"
trongrid_api_key = "132598a9-92c3-49d0-8ef2-2abc42a92d25"

# USDT contract (TRC20)
usdt_contract = "TR7NHqjeKQxGTCi8q8ZY4pL8otSzgjLj6t"

# Request timeout (seconds)
request_timeout = 15.0

# Logging
log_level = "INFO"
log_file = "logs/copilot.log"

# Safety / humanization
safety_enable = true

# AI provider (Optional / Placeholder for future expansion)
ai_api_base = "https://dashscope.aliyuncs.com/compatible-mode/v1"
ai_api_key = ""
ai_model = "qwen-plus"
ai_provider = "openai"
